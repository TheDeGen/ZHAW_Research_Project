datasets
joblib
lightgbm
matplotlib
numpy
pandas
python-dotenv
requests
scikit-learn
scipy
seaborn
sentence-transformers
torch
transformers
tqdm
umap-learn
xgboost
pyarrow
fsspec
# GPU packages (Linux only) - Install via conda for best compatibility:
# conda install -c rapidsai -c conda-forge -c nvidia cuml-cu12 cupy
# OR use pip with RAPIDS index:
# pip install cupy-cuda12x cuml-cu12 --extra-index-url=https://pypi.nvidia.com
# cupy-cuda12x; platform_system != "Darwin"
# cuml-cu12; platform_system != "Darwin"